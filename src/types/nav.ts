export class ObjectNav {
    txt: string;
    icon: string;
    to: string;
  
    constructor(txt: string, icon: string, to: string) {
      this.txt = txt;
      this.icon = icon;
      this.to = to;
    }
  }